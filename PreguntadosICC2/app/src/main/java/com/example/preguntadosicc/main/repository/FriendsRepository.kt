package com.example.preguntadosicc.main.repository

import android.app.Application
import android.content.Context

class FriendsRepository(application: Application, context: Context) {

    private val context: Context? = context
}